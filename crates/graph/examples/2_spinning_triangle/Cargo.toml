[package]
name = "spinning_triangle"
version = "0.1.0"
edition = "2021"

[dependencies]
helpers = { path = "../helpers" }
radiance-graph = { path = "../.." }

bytemuck = { version = "1.13.1", features = ["derive"] }
